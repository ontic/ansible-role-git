# {{ ansible_managed }}

{% if item.git_config %}
{% for section, options in item.git_config.iteritems() %}
[{{ section }}]
{% for key, value in options.iteritems() %}
{{ key }} = {{ value }}
{% endfor %}
{% endfor %}
{% endif %}
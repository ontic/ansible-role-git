# {{ ansible_managed }}

{% if item.git_config %}
{% for section, options in item.git_config.items() %}
[{{ section }}]
{% for key, value in options.items() %}
{{ key }} = {{ value }}
{% endfor %}
{% endfor %}
{% endif %}
